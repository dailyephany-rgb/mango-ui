rules_version = '2';
/**
 * Engineering Firestore (mango-engineering)
 * Signed-in Firebase Auth users only (eng project Auth).
 */
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
